---
output: 
  pagedown::html_paged:
    css: www/styles_weeklyQC.css
    toc: false
    numbered_sections: false
---

```{r source2, include = FALSE}
#params <- data.frame(park = "MABI", year = 2018, plot = "MABI-001")
#source("Previous_Visit_FieldForms_NETN_compile.R")
```

#### Tree Map 

```{r treemap, fig.width = 6.5, fig.height = 6}
plotTreeMap(park = params$park, from = as.numeric(params$year), 
            to = as.numeric(params$year), plotName = params$plot)


```

```{r treesum, include = F}
tree_sump <- tree_sum |> filter(Plot_Name %in% params$plot) 

tree_sum_tab <- kable(tree_sump |> select(ScientificName, Status, num_stems),
                      col.names = c("Species", "Status", "Num Stems"),
                      format = 'html', align = 'c') |> 
                kable_styling(fixed_thead = TRUE, position = 'left')
                

```

#### Summary of Trees by Status 
```{r treesum_tab}
tree_sum_tab
```

